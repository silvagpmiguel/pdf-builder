name: Manual Release OSSRH
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      OSSRH_USERNAME: ${{ secrets.OSSRH_USERNAME }}
      OSSRH_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
      OSSRH_GPG_PASS: ${{ secrets.MAVEN_GPG_PASSWORD }}
      OSSRH_GPG_PRIVKEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
      USER_EMAIL: silva.gp.miguel@gmail.com
      USER_NAME: silvagpmiguel
    steps:
      - uses: actions/checkout@v2

      - name: Set up Maven Central Repository
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
          server-id: ossrh
          server-username: OSSRH_USERNAME
          server-password: OSSRH_PASSWORD
          gpg-private-key: OSSRH_GPG_PRIVKEY
          gpg-passphrase: OSSRH_GPG_PASS

      - name: Configure Git user
        run: |
          git config user.email $USER_EMAIL
          git config user.name $USER_NAME

      - name: Delivery Snapshot
        run: mvn -B clean deploy -Pci-cd

      - name: Prepare Release Candidates
        run: mvn -B release:clean release:prepare -Pci-cd

      - name: Perfom Release
        run: mvn -B release:perform -Pci-cd